name: Issue Auto Reply

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - name: Comment on new issue
        uses: actions/github-script@v7
        with:
          script: |
            const body = `
            👋 Thanks for your report! / Bildiriminiz için teşekkürler! 🙌

            We will review this as soon as possible.
            En kısa sürede inceleyeceğiz.

            If possible, please add:
            - steps to reproduce / tekrar etme adımları
            - screenshots or logs / ekran görüntüsü veya loglar
            - browser + OS / tarayıcı + işletim sistemi
            `;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.issue.number,
              body
            });
